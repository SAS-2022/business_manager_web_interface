rules_version = '2';

// Emulator-only rules for local testing. Not deployed to production.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
