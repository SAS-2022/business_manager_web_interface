rules_version = '2';

// Emulator-only rules for local Stage 2+ testing. Not deployed to production.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
